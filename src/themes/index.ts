import { createTheme } from '@mui/material/styles';

export const lightTheme = createTheme({
  palette: {
    mode: 'light',
    // Defina cores adicionais conforme necessário
  },
  // Outras personalizações do tema
});

export const darkTheme = createTheme({
  palette: {
    mode: 'dark',
    // Defina cores adicionais conforme necessário
  },
  // Outras personalizações do tema
});